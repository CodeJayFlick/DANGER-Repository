class LldbBreakpointDisp:
    KEEP = "KEEP"
    DEL = "DEL"
    OTHER = "OTHER"

# Note: In Python, we don't need an enum class for this purpose.
# We can simply define a string constant. The above is just to mimic
# the Java code's structure and naming convention.
