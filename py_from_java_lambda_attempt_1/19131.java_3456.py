Here is the translation of the given Java code into equivalent Python:

```Python
class TagName:
    def __init__(self, name):
        self.name = name

def tag_name_of(name: str) -> 'TagName':
    return TagName(name)

```

Please note that this Python code does not include any type hints or annotations like the original Java code.