Here is a translation of the Java code into equivalent Python:

```Python
import os
from abc import ABCMeta, abstractmethod


class ResourceFileJavaFileManager:
    def __init__(self, source_dirs: list, files_to_avoid: set):
        self.source_dirs = source_dirs
        self.files_to_avoid = files_to_avoid

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs, files_to_avoid)
        self.file_manager = None

    @abstractmethod
    def is_supported_option(self, option: str) -> int:
        pass


class ResourceFileJavaFileManager(ResourceFileJavaFileManager):

    def __init__(self, source_dirs: list, files_to_avoid: set):
        super().__init__(source_dirs,