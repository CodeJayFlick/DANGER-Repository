Here's how you can translate this Java enum into a Python class:

```Python
class DbgBreakpointDisp:
    KEEP = "KEEP"
    DEL = "DEL"
    OTHER = "OTHER"

# You can use it like this:
print(DbgBreakpointDisp.KEEP)  # prints: KEEP
```

This is equivalent to the Java enum, but in Python.