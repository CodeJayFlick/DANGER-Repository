Here is the translation of the given Java enum into a Python equivalent:

```Python
class BootImageLiveObjects:
    OOME_WHEN_THROWING_EXCEPTION = 1
    OOME_WHEN_THROWING_OOME = 2
    OOME_WHEN_HANDLING_STACK_OVERFLOW = 3
    NO_CLASS_DEF_FOUND_ERROR = 4
    CLEARED_JNI_WEAK_SENTINEL = 5

    INTRINSIC_OBJECTS_START = 6
```

This Python code defines a class `BootImageLiveObjects` with six attributes. Each attribute is an integer value, similar to the Java enum values in your original code.