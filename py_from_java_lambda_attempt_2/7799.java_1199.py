   0x101003  program; ProgramBuilder 0x101001  program 0x100000L;
Program get
    a8
*/ 0x1010x1010x1010x1010x1010x1010x1Model; Program get 0x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x1010x
* Program get 
	} }  // end of test

}

// Test the methods with different parameters.
public void testRtti() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(28, rtti.getSize());
    assertEquals(true, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(false);
    assertEquals(false, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti2() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti3() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti4() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti5() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti6() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti7() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti8() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti9() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti10() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti11() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti12() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti13() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti14() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti15() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti16() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti17() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti18() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti19() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti20() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti21() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti22() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti23() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30, rtti.getSize());

    rtti.setIsFollowFlow(true);
    assertEquals(true, rtti.isFollowFlow());
}

// Test the methods with different parameters.
public void testRtti24() {
    Rtti rtti = new Rtti();
    assertEquals("Base", rtti.getBase());
    assertEquals(20, rtti.getSize());

    rtti.setBase("Circle");
    assertEquals("Circle", rtti.getBase());
    assertEquals(false, rtti.isFollowFlow());

    rtti.setSize(30);
    assertEquals(30,