  get
   * 	*   get
*/      one
          get
  public
/*  get
*/
    and   get 	// instance;
   get
*/     */get
	*0
			//instance; get
*   get
       *   get
            get
*/  get
       //      and
	* get
 /*

 get
  get
*   get
/*  get
}
} } }

public class PDB {
    public static void main(String[] args) {

        new PDB().execute();
    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseMoved(MouseEvent e) {

    }
}

class PDB extends JPanel implements ActionListener, MouseListener, MouseMotionListener {
    private JFrame frame;
    private JTable table;

    public PDB() {
        frame = new JFrame("PDB");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.add(new JScrollPane(table));
        frame.pack();
        frame.setVisible(true);

        addMouseListener(this);
        addMouseMotionListener(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void mouseClicked(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseEntered(MouseEvent e) {
        // nothing to do here, just a placeholder for now
    }

    @Override
    public void mouseExited(MouseEvent e) {
        // nothing to do here, just a placeholder for now
