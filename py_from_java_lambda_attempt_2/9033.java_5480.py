Here is the translation of the given Java code into equivalent Python:

```Python
class VTRelatedMatchCorrelationType:
    CALLER = 1
    TARGET = 2
    CALLEE = 3
    UNRELATED = 4
```

This Python class defines an enumeration (enum) for `VTRelatedMatchCorrelationType` with the same values as in Java.